FRIM (FRactal Iterative Method) is a software to generate/recover random
fields with stationnary covariance.

There build Yorick plugin for FRIM:

  cd src
  yorick -batch make.i
  make clean
  make
  make install


